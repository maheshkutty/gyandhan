import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      red1: {
        main: "#ee3a5a",
        contrastText: "#fff",
      },
      blue1: {
        main: "#2c95a599",
        contrastText: "#fff",
      },
    },
  });

export default theme; 
